# Notebooks

These notebooks are intended to stay output-free in git. Run them locally to inspect
the test CAD cases with the `jupyter_cadquery` widget.

Use the project conda environment:

```bash
/mnt/D/packages/miniconda3/envs/game-qt6-py312/bin/python -m jupyter lab
```
